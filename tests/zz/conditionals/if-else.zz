let a: int64_t = 15;
let f: double = 12.34;
let u: uint16_t = 15;

fn main() {
    if (a >= 20) {
        let res: int64_t = a - 5;
    }
    else {
        let res: int64_t = a + 5;
    }

    if (f != 0.0) {
        let val: double = f / 2.0;
    }
    else {
        let val: double = 0.0;
    }

    if (u < a) {
        let x: uint16_t = u * 2;
    }
    else {
        let x: uint16_t = u / 2;
    }
}
