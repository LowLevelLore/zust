let a: uint32_t = 123;
let b: float = 12.3f;

let z: integer = 0;

if(a > b + 10){
    // In variable access, its being treated as a stack based local variable.
    // Also see that Integer literals are convertible to uint* and int*
    z = 69;
}